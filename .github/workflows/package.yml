name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-2019

    steps:
    - uses: actions/checkout@v2
    - name: make
      working-directory: src
      run: make
    #- name: make check
    #  run: make check
    #- name: make distcheck
    #  run: make distcheck
